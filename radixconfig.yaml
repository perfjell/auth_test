apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: auth_test
spec:
  environments:
    - name: dev
      build:
        from: master
    - name: prod
  components:
    - name: api.py
      src: "."
      publicPort: http
      ports:
       - name: http
         port: 5000
